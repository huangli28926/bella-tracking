# bella-tracking_v4 小白使用手册

（对照当前 `SKILL.md`，2026-09-01）

用一句话：**把「埋点需求 Excel」交给对话里的 AI，它帮你分析落点、打开网页让你确认、按确认结果写代码、再用浏览器自动点一遍验收。**

你不需要会跑脚本。多数时候只要：**把 Excel 放到 `docs/` → 在对话里选入口 → 在弹出的网页上点确认。**

给 Agent 的操作规范在同目录 `SKILL.md`。本文件只讲**你要做什么**。

---

## 1. 它能帮你做什么

| 你想要的结果 | 选 |
|---|---|
| 每条埋点落在哪个文件、点什么发、参数从哪来 | **A**（入口 3，或全流程里的第一步） |
| 拿不准的地方，网页里改完再勾确认 | **B**（不单独出现在菜单里） |
| 把确认过的埋点写进业务代码 | **C**（入口 1 / 2 说「进入 C」之后） |
| 自动点页面，核对有没有发出、uicode 对不对 | **D**（入口 4） |
| 验收挂了，修一轮空值/表达式 | **E**（入口 5，须你点名） |
| 扫仓库已有埋点，画出页面和跳转 | **F**（入口 6） |
| 对照某份 Excel：文档有、代码扫描没有的 evtId | **7 缺失列表** |
| 按缺失表把历史缺的点补进代码（不自动验收） | **8 补全历史** |

公司统一上报是 dig-log（`$ULOG.send`）。仓库若已有封装（例如 `sendLog`），AI 会跟现有写法，**不会为了统一去改旧页，也不会为了对账把旧点删掉重写。**

**本期新埋点**（没开补历史模式）：同一 evtId 若旧页和新页都能进，**优先落在本期改过的文件上**，验收只走锁定的那一条链。查找仍会搜整个前端源码，不是只扫本期 diff。

**补历史缺失**（入口 8，建议 `.env` 写 `trackingMode=backfill`）：写回示意图对应的旧页，**不要**为了新入口把历史 evtId 写到新文件。

---

## 2. 开始前你要准备什么

1. **埋点需求 Excel**（xlsx）。
2. **本仓库能打开**（能登录；验收按 `.env` 地址打开页面）。
3. **Cursor 对话加载本 Skill**（`/bella-tracking_v4`）。
4. 验收会用本机 **Chrome + Playwright**；缺依赖交给 AI 装。

**Excel 必须在仓库根 `docs/` 下。**

- 没有 `docs/`：AI 会建。
- 文件在桌面/下载：应**搬进** `docs/原文件名.xlsx`。
- `docs/` 已有**同名但内容不同**的文件：报错，禁止覆盖，请先改名。

对话里用：`--excel=docs/你的文件名.xlsx`

选 **7 / 8** 时如果没给可用的 xlsx（也没有已生成的缺失 JSON），以脚本 `prompts.ASK_HISTORY_EXCEL` / `--status` 的 `prompt` 为准原样询问。未回复不得开扫、不得对账、不得写码。

---

## 3. 你怎么跟 AI 说话（最重要）

没说跑哪一段时，AI **必须先跑** `tracking-workflow --status --json`，把返回的 `prompt`（`prompts.ENTRY_MENU`）原样发给你，不能自己开跑或改写选项。

| 你的情况 | 选 |
|---|---|
| 从零到验收（**推荐新人做本期新埋点**） | **1** |
| 分析 + 确认 + 写代码，先不自动点页面 | **2** |
| 只要分析，**先不要改业务代码** | **3** |
| 落库和代码都好了，只跑验收 | **4** |
| 刚验收失败，同意修一轮空值 | **5** |
| 历史埋点关系图 | **6** |
| 看哪些 evtId 文档有、扫描结果没有 | **7** |
| 把 7 列出的真实缺失补进代码 | **8** |

口头对照：

- 「只跑 A」→ 只分析，停，不再列菜单。
- 「落库到写码 / A→B→C」→ 写完代码就停。
- 「验收全流程」→ A 到 D。**E、F、7、8 都不会自动进。**
- 「补全历史埋点 / 入口 8」→ 路径 H：先有缺失表，假缺失分流后只写 `missing`，**不自动验收**。

菜单里没有从零的「只跑 B / 只跑 C」。口头说了会先检查产物，缺 A 就完整做 A（含分析，不只抽 Excel）。

---

## 4. 仓库根 `.env`

| 字段 | 作用 | 小白建议 |
|---|---|---|
| `baseUrl` | 站点 origin | 验收前必须有 |
| `seedUrl` | **完整** path + query，不要删参数 | 验收前必须有 |
| `acceptDevice` | `mobile` 或 `pc` | 写了验收不用每次问设备 |
| `trackingBaseline` | 和哪根分支比本期改了哪些文件 | **不写默认 `master`** |
| `trackingMode` | 只有写成 `backfill` 才是「补历史、旧页优先」 | **不写 = 未配置**，禁止猜成 backfill |

### 本期入口（`trackingBaseline`）

适用于**没写** `trackingMode=backfill` 的本期新埋点。

- `git diff {trackingBaseline}...HEAD` 得到本期文件。
- 全仓库都能搜到；**多处命中时优先本期文件**，也可以落在 diff 外（全局结果仍有效）。
- 旧页上加了新埋点：会倒推跳转边。有**本期新增跳转**就只验这条新边；跳转都是老的，就从 `seedUrl` 走已有最短路径进旧页再点，**不会编新入口，也不会把所有老入口都点一遍**。
- 种子页已经是落点页 → 没有前置点击。
- 仓内扫不到入边 → 以 seed 直达为准。
- 基线分支不存在会停下来问你。
- **验收脚本不读** `trackingBaseline`，只跑 `impl` 里已经锁死的那条链。

### 补历史（`trackingMode=backfill`）

仅在你**明确要补历史缺失**（入口 8）时建议加上。

- 多入口优先：示意图文案、同模块已落地的文件，**不要求**文件在本期 diff 里。
- 禁止为了走新入口把历史 evtId 写到新页。
- 即使分支上有新跳转，验收主路径也不走「新跳转」，改用旧最短路径 / 种子已在该页 / 无入边。
- 入口 8 开跑前：要用旧页优先，AI 应提示你加上这一项；不加则仍全局查找 + 本期优先，但**写码范围仍只限缺失列表**。

---

## 5. 一条龙（选 1：本期新埋点）

```
A 分析 Excel + 看图搜代码（全局搜，多入口优先本期）
    ↓ 拿不准：本轮第一次弹出浏览器；后续条在同一页切换
B 扫尾 + 打开整份落库页
    ↓ 你回复「进入 C」
C 只新增本期调用（或同一 evtId 原地改参）
    ↓ 若检测到要删旧埋点，先问你保留还是确认删除
D 问手机还是 PC → 自动点关键路径
    ↓ 无论过没过都会出终稿；有失败再问自修复 / 人工矫正
E 最多 1 轮（不会偷偷连跑）
```

选 **8** 是另一条链：入口 7 缺失表 → 假缺失分流 → 只对真实缺失走 A→B→C → 再跑一遍 7 核对。**默认停在写码，不进 D。**

---

## 6. 阶段 A：落库

### AI 在做什么

1. 读 Excel，下示意图（可缓存）。不要把此时的空 HTML 当你成品。
2. 探测本仓库怎么发埋点。
3. 点击 / 曝光 **按文档顺序一起**分析。
4. 全仓库搜 evtId 和示意图文案；未开 backfill 时多入口优先本期文件。
5. 同文档已确认的同名参数可能被记住填到后面（见「参数记忆」），把握中等，**仍须你确认**。
6. 写 `impl.json`，生成落库 HTML。

### 什么时候会打断你

任一即打断并走矫正向导：

- 找不到文件 / 状态未解决
- 参数空，或把握中/低
- 用了记忆回填
- 文档 uicode 和落点路由不一致

**本轮第一次打断必须打开系统浏览器。** 之后同一服务已在跑时，**不再新开标签**，向导会在当前页切到下一条。禁止整轮不打开浏览器、禁止只发链接、禁止只开本地静态 HTML。

地址类似：`http://127.0.0.1:3920/{文档名}-落库.html?evt=事件ID&mode=confirm`

向导等太久超时：会再强制打开一次该条；聊天里只报 **事件 ID、事件名、待确认短句、URL**。

### 你在向导里做什么

1. 看事件 ID、名称、待确认项（位置 / 某个参数 / uicode 冲突）。
2. 参数可以填 **JS 表达式**，也可以填 **备注**（人话说明怎么取值，给写码当提示，不必长得像代码）。备注非空也算已经给了取值。
3. 改完后 **勾选「已确认落库内容」**，再点「确认并继续」。
4. 也可「跳过稍后处理」。

全部参数高把握且落点明确 → 不打断。

### 参数记忆

前面确认过的同名参数可能自动带到后面：

- 不是跳过确认。
- 不覆盖已经是高把握且有值的表达式。
- 代码里已有调用优先于记忆。
- 不决定落点文件和 uicode。

### A 的产物（`docs/tracking/impl/{文档名}/`）

| 文件 | 是什么 |
|---|---|
| `{文档名}-落库.html` | 可视化页（请用本地服务保存） |
| `_raw/{文档名}.events.json` | Excel 抽出的事件（uicode 以它为准） |
| `_raw/{文档名}.impl.json` | **主真相**（落点、参数、片段） |
| `_raw/images/{事件ID}.png` | 示意图 |
| `_raw/field-memory.json` | 已确认参数记忆 |

只跑 A 会停，不改业务代码。

---

## 7. 阶段 B：人工矫正

未确认的条按顺序在**同一向导页**继续（第一条才开浏览器）。

队列清空后生成 `{文档名}-矫正.html`。

**进 C 前必做：** 打开**整份**落库页（不是单条向导），你回复 **「进入 C」**。队列已空也不能跳过。

---

## 8. 阶段 C：写业务埋点

选 1 或 2 并且说「进入 C」= 同意改业务代码。

AI 会：

- 按 `impl.json` 写调用；封装跟落点文件已有写法。
- **uicode 抄文档**，不用现网旧值盖文档。
- 参数若是备注（人话）：会在该文件里追真实变量再写入，**不会把备注当 JS 粘贴**。
- 未开 backfill：写已定的 `targetFile`（优先本期），不为「多种进入方式」去改无关旧页。
- 开了 backfill：写历史 UI 所在文件，不为新入口改到新页。
- **不删除旧埋点**（包括文档没列的 evtId）。已有的点只对账/补参，不整段删掉挪走。

### 若检测到要删旧埋点

AI 必须停下，把 `check-old-tracking` / `prompts.formatDeleteOldTracking` 打出的二选一原样发给你。选 1 → 撤回删除。选 2 → 才允许删并继续。没回复不得继续。

交付：代码 diff + 更新后的验收字段。若你确认过删除，交付里会写明 evtId。

---

## 9. 阶段 D：关键验收

只走编好的关键路径，不整页乱点。

### 先选设备

命令和 `.env` 都没有设备时必须问，**禁止默认 iPhone**。文案以 `prompts.DEVICE_PROMPT` / `D_CHOOSE_DEVICE.prompt` 为准。

也可写 `acceptDevice=mobile|pc`。只出计划（plan-only）可不选设备。

遇登录会先登录，态保存在 `docs/tracking/.auth/storage.json`。

### 怎样记结果（不是「全绿才算出过」）

- 必须发出对应事件 ID 和类型；`uicode` 以文档为准。
- 必须抓到上报像素请求（测试 `check.gif` / 线上 `alliance.gif`）。**请求发出就算捕获**（跳页导致没收到响应也算）。完全没请求才失败。HTTP 非 2xx 只展示，不改成败。
- 参数是 `-` / 空 / 缺失：**不判失败**，标成需你看的空值。
- 某次点击把后面的点挡掉（离开页面、下一条找不到）：脚本把**挡路那条标成路径跳过**，回到种子页继续后面的点。你不用事先勾选。漏判时 AI 最多再跑 1 轮指定跳过，仍算 D，不算 E。
- **没点下去**（找不到按钮）仍是失败，不会为了「怕挡住后面」改成跳过。

**终稿是结果快照，不是全绿奖状。** 真实验收无论失败/跳过多少，都会生成 `{文档名}-终稿.html` 并打开。plan-only 不生成终稿。

每条一般三张图：点前按钮图、按钮所在整页图、需求示意图。报告里有当时完整页面 URL，可复制。

### 仍有失败时怎么选

文案以 `prompts.D_FAIL_CHOICE` / `D_CHOOSE_REPAIR.prompt` 为准（自修复 / 直接人工矫正）。未选前不改代码、不进 E、不打开 B。

---

## 10. 阶段 E：空值自修复

仅：D 失败后你选 1，或你说「自修复 / 修空值」。

- 最多 1 轮。
- 默认可改落库表达式、验收等待时机。
- 要改组件传参或产品没定取值：先给方案，你同意才改代码。
- 数据本身就是空：只标注，不改代码。
- 再失败 → 自动打开人工矫正（优先失败那条，必要时强制开浏览器）。

---

## 11. 阶段 F：历史埋点关系

说「梳理历史埋点 / 关系图」视为选 F，AI 仍应先把扫描命令发给你，**你确认后再跑**。不读 `seedUrl`。

产物：`docs/historyTracking/年/月日_时分秒.html`  
不改业务代码。

---

## 12. 入口 7：缺失埋点列表（只读）

对照 Excel 的 evtId 和仓库扫描结果，**只比字面量 ID**。

- 文档有、扫描没有 → 列入缺失。
- 动态拼接、注释掉的调用，可能被**误报成缺失**。
- 交付：控制台 + `{文档名}-缺失埋点.html` / `.json`。
- **列表不是写码许可。** 要补全走入口 8。

---

## 13. 入口 8 / 路径 H：补全历史缺失

依赖入口 7 的缺失表。没有表会先跑 7。缺失为 0 则停，不改代码。

对每条「缺失」，AI 会先分流，避免把扫描误报写成字面量骗绿：

| 类型 | 含义 | 你这边 |
|---|---|---|
| 动态 ID | 运行时拼出来的，不是字面量 | 不当真缺，不新增字面量调用 |
| 整段注释 | 代码里注释掉了 | 问你：恢复注释，还是按现写法重开 |
| 功能已下线 | 页面/入口没了 | 你确认后跳过 |
| 真实缺失 | 确实没写 | 只对这些走 A→B→C |

然后与本期流程相同：分析这些 evtId → 向导确认 → 整页「进入 C」→ **只新增缺失**（或原地改参），不改已经 found 的调用。写完再跑一遍入口 7 核对。

你点名才跑 D，且仍要选设备。入口 8 **不会**自动进 D/E。

要用旧页优先时加上 `trackingMode=backfill`。

---

## 14. 推荐对话（可复制）

**本期从零到验收：**  
> Excel 在 `docs/2.3埋点需求文档.xlsx`，走验收全流程。

**只分析不改代码：**  
> 只跑 A，excel 是 `docs/2.3埋点需求文档.xlsx`。

**确认页看完了：**  
> 进入 C。

**验收用手机：**  
> 用移动端（iPhone 13）跑 D。

**验收有失败，先自动修一轮：**  
> 选 1，自修复。

**自己在网页改：**  
> 选 2，打开人工矫正。

**主分支不是 master：**  
> 本期相对 `origin/dev`，把 `trackingBaseline` 设成它再跑 A。

**看哪些历史点没落地：**  
> 选 7，excel 是 `docs/2.6埋点需求文档.xlsx`。

**按缺失表补旧页：**  
> 选 8。请加上 `trackingMode=backfill`，补完先不要跑验收。

**发现要删旧点，我要留着：**  
> 选 1，保留旧埋点。

---

## 15. 矫正向导按钮

| 操作 | 含义 |
|---|---|
| 勾选「已确认落库内容」 | 这条你可以签字 |
| 确认并继续 | 记下已确认，同页下一条；全部完成到矫正汇总页 |
| 跳过稍后处理 | 先跳过，后面还会扫到 |

服务一般在 `http://127.0.0.1:3920`（占用会试到 3930）。**必须用服务页保存**；资源管理器双击 HTML 通常存不回去。

---

## 16. 原则（尽量记住这几条）

1. 不要用 Excel「页面名称」当真实路由。
2. 不要改文档 uicode；不一致只记冲突。
3. 参数拿不准宁可空着或写备注；不要编 `props.xxx` / `window.xxx`（`window.__user` 除外）。
4. 主数据是 `impl.json`，只改 HTML 不算数。
5. 关掉对话也能续：同一 Excel，读磁盘进度。
6. 本期新埋点：多入口优先本期；补历史且 `backfill`：写旧页。
7. 写码默认**只加不删**；要删旧点必须你确认。
8. 终稿是验收快照，失败也会出。

---

## 17. 常见问题

**Q：只丢了 xlsx，AI 自己开始分析了？**  
A：应先出 8 项菜单。让它停下并列出选项。

**Q：后面几条没再弹新窗口？**  
A：正常。第一次打开后，后续在同一向导页切换。超时才会再强制打开。

**Q：双击落库 HTML 改完刷新没了？**  
A：用 `127.0.0.1:3920` 服务页。

**Q：参数我只会写人话，不会写 JS？**  
A：向导里填备注即可。写码时 AI 会按备注去文件里找真实变量。

**Q：验收为什么要问手机还是 PC？**  
A：视口不同。未选择不得默认 iPhone。

**Q：上报是 `-` 算失败吗？**  
A：不算，会标成需确认的空值。

**Q：有失败还会出终稿吗？**  
A：会。终稿不是全绿奖状。

**Q：全流程会自动修空值 / 补历史吗？**  
A：不会。E 要你选；7 / 8 要另选。

**Q：入口 7 列出缺失，会直接改代码吗？**  
A：不会。补全选 8，仍须「进入 C」。没写 `trackingMode` 不会默认 backfill。

**Q：动态拼出来的 evtId，入口 7 说缺失？**  
A：可能是假缺失。入口 8 会标出来，不要改成字面量骗绿。

**Q：旧首页和新首页都能点同一个点，会改旧首页吗？**  
A：本期新埋点优先写本期文件。补历史且 `backfill` 才写回旧页。

**Q：我不会命令行？**  
A：Excel 放到 `docs/`，用本 Skill 对话即可。

---

## 18. 阶段门禁（可跳过）

| 阶段 | 谁做 | 怎样算完成 |
|---|---|---|
| A | 脚本 + AI | 抽出事件、探测封装、填好 impl、落库页在、校验无 error；该确认的已走浏览器向导 |
| B | 你 | 待确认队列处理完；进 C 前看过整份落库页并说「进入 C」 |
| C | AI | 你同意改代码；只增不乱删；验收字段补全；关键路径无卡住的 pending |
| D | 脚本 | 真实验收（非只出计划）已跑完并生成终稿（允许有失败/路径跳过） |
| E | AI | 仅你同意；最多 1 轮 |
| 7 | 脚本 | 给出缺失 HTML/JSON；不改代码 |
| H（8） | AI + 你 | 有缺失表；假缺失分流；只写真实缺失；再跑 7；不自动验收 |

中断后续跑：同一 `--excel=`，让 AI 看进度即可。
